# must accept EULA the first time that the image is started# docker run splunk/enterprise:6.5.3-monitor# Options on how to review the EULA and accept it: # 1. docker run -it splunk/enterprise:6.5.3-monitor# 2. Add the following environment variable: SPLUNK_START_ARGS=--accept-license# e.g., docker run -e "SPLUNK_START_ARGS=--accept-license" splunk/enterprise:6.5.3-monitor version: '3'volumes:  opt-splunk-var:  uf-opt-splunk-var:  dep-opt-splunk-var:services:   splunkenterprise:#    build: .    hostname: splunkenterprise    image: splunk/splunk:7.0.0-monitor    environment:      SPLUNK_START_ARGS: --accept-license --answer-yes      SPLUNK_ENABLE_LISTEN: 9997      SPLUNK_ADD: tcp 1514      SPLUNK_USER: root      SPLUNK_DEPLOYMENT_SERVER: 'splunkdeploymentserver:8089'      SPLUNK_ADD_1: "index docker"      SPLUNK_ADD_2: "index mysql_logs"    volumes:      - opt-splunk-var:/opt/splunk/var      - ~/docker/splunk_architecture/enterprise_etc:/opt/splunk/etc:rw#      - /var/log:/docker/log:ro    ports:      - "8000:8000"      - "9997:9997"      - "28089:8089"      - "8088:8088"      - "1514:1514"  # openldap:    # image: osixia/openldap:1.1.11    # container_name: openldap    # environment:      # LDAP_LOG_LEVEL: "256"      # LDAP_ORGANISATION: "Example Inc."      # LDAP_DOMAIN: "example.org"      # LDAP_BASE_DN: ""      # LDAP_ADMIN_PASSWORD: "admin"      # LDAP_CONFIG_PASSWORD: "config"      # LDAP_READONLY_USER: "false"      # #LDAP_READONLY_USER_USERNAME: "readonly"      # #LDAP_READONLY_USER_PASSWORD: "readonly"      # LDAP_RFC2307BIS_SCHEMA: "false"      # LDAP_BACKEND: "hdb"      # LDAP_TLS: "true"      # LDAP_TLS_CRT_FILENAME: "ldap.crt"      # LDAP_TLS_KEY_FILENAME: "ldap.key"      # LDAP_TLS_CA_CRT_FILENAME: "ca.crt"      # LDAP_TLS_ENFORCE: "false"      # LDAP_TLS_CIPHER_SUITE: "SECURE256:-VERS-SSL3.0"      # LDAP_TLS_PROTOCOL_MIN: "3.1"      # LDAP_TLS_VERIFY_CLIENT: "demand"      # LDAP_REPLICATION: "false"      # #LDAP_REPLICATION_CONFIG_SYNCPROV: "binddn="cn=admin,cn=config" bindmethod=simple credentials=$LDAP_CONFIG_PASSWORD searchbase="cn=config" type=refreshAndPersist retry="60 +" timeout=1 starttls=critical"      # #LDAP_REPLICATION_DB_SYNCPROV: "binddn="cn=admin,$LDAP_BASE_DN" bindmethod=simple credentials=$LDAP_ADMIN_PASSWORD searchbase="$LDAP_BASE_DN" type=refreshAndPersist interval=00:00:00:10 retry="60 +" timeout=1 starttls=critical"      # #LDAP_REPLICATION_HOSTS: "#PYTHON2BASH:['ldap://ldap.example.org','ldap://ldap2.example.org']"      # KEEP_EXISTING_CONFIG: "false"      # LDAP_REMOVE_CONFIG_AFTER_SETUP: "true"      # LDAP_SSL_HELPER_PREFIX: "ldap"    # tty: true    # stdin_open: true    # volumes:      # - /var/lib/ldap      # - /etc/ldap/slapd.d      # - /container/service/slapd/assets/certs/    # ports:      # - "389:389"      # - "636:636"    # domainname: "example.org" # important: same as hostname    # hostname: "example.org"  # phpldapadmin:    # image: osixia/phpldapadmin:latest    # container_name: phpldapadmin    # environment:      # PHPLDAPADMIN_LDAP_HOSTS: "openldap"      # PHPLDAPADMIN_HTTPS: "false"    # ports:      # - "8080:80"    # depends_on:      # - openldap   splunkuniversalforwarder:    hostname: splunkuniversalforwarder    image: splunk/universalforwarder:7.0.0    environment:       SPLUNK_START_ARGS: --accept-license --answer-yes      SPLUNK_FORWARD_SERVER: 'splunkenterprise:9997'      SPLUNK_FORWARD_SERVER_ARGS: '-method clone'      SPLUNK_DEPLOYMENT_SERVER: 'splunkdeploymentserver:8089'          volumes:      - uf-opt-splunk-var:/opt/splunk/var      - /var/lib/docker/containers:/host/containers:ro      - /var/run/docker.sock:/var/run/docker.sock:ro      - ~/docker/splunk_architecture/ufetc:/opt/splunk/etc:rw    ports:      - "18089:8089"      - "11514:1514"   splunkdeploymentserver:    hostname: splunkdeploymentserver    image: splunk/splunk:7.0.0    environment:      SPLUNK_START_ARGS: --accept-license --answer-yes      SPLUNK_FORWARD_SERVER: 'splunkenterprise:9997'      SPLUNK_FORWARD_SERVER_ARGS: '-method clone'      SPLUNK_ADD: tcp 1514      SPLUNK_USER: root    volumes:      - dep-opt-splunk-var:/opt/splunk/var      - ~/docker/splunk_architecture/deploy_etc:/opt/splunk/etc:rw#      - /var/log:/docker/log:ro    ports:      - "49997:9997"      - "48000:8000"       - "48089:8089"      - "48088:8088"      - "41514:1514"